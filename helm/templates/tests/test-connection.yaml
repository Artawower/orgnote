apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "roam.fullname" . }}-test-connection"
  labels:
    {{- include "roam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "roam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
